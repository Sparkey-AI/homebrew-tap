# Sparkey Homebrew tap

```sh
brew install sparkey-ai/tap/sparkey
```

Formula only — no source. Binaries are downloaded from the public release
mirror at https://install.sparkey.ai and verified against the SHA256 the
release pipeline publishes in `latest.json`. The formula self-updates from
that file on a schedule.
